# 第6章 セキュリティ管理

---

## 6-1. 暗号化キーの管理

### 暗号化キーとは

組合員の口座情報（口座番号・口座名義）はAES-256で暗号化されてデータベースに保存されています。
この暗号化に使用するキーファイルが `secret.key` です。

| 項目 | 内容 |
|------|------|
| ファイルパス | `%APPDATA%\AccountReconciler\secret.key` |
| 保護方式 | Windows DPAPI（そのPC・ユーザーアカウントに紐付け） |
| 自動生成 | 初回起動時に自動生成される |

### 重要な取り扱いルール

> **このファイルを削除・移動すると、登録済みの全口座情報が復号できなくなります。**

- **削除しない**：OSの再インストールや不用意なファイル削除に注意
- **直接コピーしない**：別PCにコピーしても、DPAPIの保護によりそのPCでは読めません
- **PC移行時**：必ずアプリのバックアップ機能を使用します（→[第7章](07_backup_migration.md)）

### キーが破損・削除された場合

キーファイルが失われた場合、**登録済みの口座情報は復号不可能になります。**

対処方法：
1. バックアップZIPが存在する場合 → リストアにより復元可能（→[第7章](07_backup_migration.md)）
2. バックアップが存在しない場合 → 口座情報を再登録するしかありません

**これが定期バックアップが重要な理由です。**

---

## 6-2. APIトークンの管理

### APIトークンとは

KintoneのAPIにアクセスするための認証トークンです。
7つのKintoneアプリそれぞれに対して1つのトークンが必要です。

### 管理上の注意事項

| 注意事項 | 内容 |
|---------|------|
| 第三者に見せない | `kintone_api_tokens.json` の内容は漏洩させない |
| gitにコミットしない | `.gitignore` に登録済みだが、誤って追加しないよう注意 |
| PC廃棄時に削除 | `%LOCALAPPDATA%\AccountReconciler\kintone_api_tokens.json` を削除 |

### APIトークンの更新・ローテーション

セキュリティ上の理由や、トークンが漏洩した可能性がある場合はトークンを再発行します。

1. Kintoneで対象アプリを開き、**「アプリの設定」→「APIトークン」** を開きます
2. 既存トークンを削除し、**「生成する」** で新しいトークンを発行します
3. **「アプリを更新」** をクリックして確定します
4. `%LOCALAPPDATA%\AccountReconciler\kintone_api_tokens.json` を更新します
5. アプリを再起動して動作確認します

> トークンを更新しても過去のデータには影響しません。

---

## 6-3. 重要ファイルのアクセス制御

以下のファイルはシステム管理者のみがアクセスできるよう管理してください。

| ファイル | 推奨対応 |
|---------|---------|
| `secret.key` | Windowsのユーザーアカウント権限で保護される（DPAPI）。共有PCの場合は専用ユーザーアカウントで運用 |
| `kintone_api_tokens.json` | 管理者のみがアクセスできるフォルダに配置 |
| バックアップZIP | パスワード保護付きストレージや外付けドライブに保管 |

---

## 6-4. PC廃棄・OSクリーンインストール時の手順

**作業前に必ずバックアップを取得してください**（→[第7章](07_backup_migration.md)）

廃棄・クリーンインストール前に削除すべきファイル：

```
%APPDATA%\AccountReconciler\secret.key
%LOCALAPPDATA%\AccountReconciler\kintone_api_tokens.json
%LOCALAPPDATA%\UnionDuesCollectionManager\SecureBank.db
```

ゴミ箱に入れるだけでなく、ファイル削除ツールやストレージの初期化で確実に消去してください。

---

[← 前章：Kintoneユーザー管理](05_kintone_user_management.md) ｜ [次章：バックアップ・PC移行 →](07_backup_migration.md)
